package com.github.${extVars.pkg}.service.${extVars.module};

import com.github.javahao.base.Service;
import com.github.${extVars.pkg}.entity.${extVars.module}.${genName};

/**
 * usedfor：[service]${table.tableComment}
 * Created by ${auth} on ${.now?string("yyyy/MM/dd HH:mm:ss")}.
 * auth：${auth}
 */
public interface ${genName}Service extends Service<${genName}>{
}
